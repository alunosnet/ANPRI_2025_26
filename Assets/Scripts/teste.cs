using UnityEngine;

public class teste : MonoBehaviour
{
    [SerializeField] string nome="Joaquim";
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log(nome);
    }

    // Update is called once per frame
    void Update()
    {

        
    }
}
